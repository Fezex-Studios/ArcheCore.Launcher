import AuthComponent from "@/components/auth/authComponent.tsx";

const Login = () => {
    return (
        <div>
            WELCOME TO THE LOGIN SCECTION
            <AuthComponent />
        </div>
    );
};

export default Login;